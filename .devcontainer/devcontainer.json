{
    "name": "blog",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "target": "dev"
    },
    "forwardPorts": [3000],
    "postCreateCommand": "rustc --version && cargo --version && rustup target list --installed"
}
