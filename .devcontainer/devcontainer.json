{
    "name": "blog",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "target": "dev"
    },
    "forwardPorts": [3000],
    "remoteUser": "vscode",
    "postCreateCommand": "rustc --version && cargo --version && rustup target list --installed"
}
